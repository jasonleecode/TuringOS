PKGDIR ?= ..
L4DIR  ?= $(PKGDIR)/../..

# Standalone net utilities (each a separate spawnable program reaching the
# network over netd IPC).
TARGET = ifconfig netcat dhcp ping nslookup udpecho tcpecho

include $(L4DIR)/mk/subdir.mk
