# net-cluster — outward connectivity suite (remote access + IoT protocols).
# Each protocol builds a small library linked into native_shell, which owns
# the lwIP network stack and registers the shell commands.
PKGDIR ?= .
L4DIR  ?= $(PKGDIR)/../..

TARGET = protocol

include $(L4DIR)/mk/subdir.mk
